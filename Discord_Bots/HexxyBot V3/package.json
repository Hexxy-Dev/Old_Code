{
  "name": "hexxybot-v3",
  "version": "1.0.0",
  "description": "The new an improved HexxyBot",
  "main": "index.js",
  "scripts": {
    "cleanBuild": "rm -r build && tsc && rsync -r -R --exclude='*.ts' ./src/./ ./build/",
    "build": "tsc",
    "start": "node index.js",
    "buildAndRun": "npm run build && npm run start"
  },
  "author": "Hexxy",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^14.7.1",
    "require-all": "^3.0.0"
  }
}
